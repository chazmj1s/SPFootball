<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:NCAA_Power_Ratings.Mobile.Models"
             xmlns:controls="clr-namespace:NCAA_Power_Ratings.Mobile.Controls"
             x:Class="NCAA_Power_Ratings.Mobile.Views.RivalriesPage"
             x:Name="ThisPage">

    <Grid RowDefinitions="Auto,Auto,*,24">

    <!-- Tier filter bar -->
    <Grid Grid.Row="0"
          ColumnDefinitions="Auto,*"
          Padding="10,6,10,4"
          ColumnSpacing="8">

      <Label Grid.Column="0"
             Text="Tier"
             FontSize="11"
             FontAttributes="Bold"
             TextColor="{AppThemeBinding Light=#555555, Dark=#AAAAAA}"
             VerticalOptions="Center" />

      <Picker Grid.Column="1"
              ItemsSource="{Binding TierFilters}"
              SelectedItem="{Binding SelectedTier}"
              FontSize="13"
              HeightRequest="32"
              VerticalOptions="Center" />
    </Grid>

    <!-- Column headers -->
    <Grid Grid.Row="1"
          ColumnDefinitions="28,*,110,90"
          Padding="10,4,10,4"
          BackgroundColor="{AppThemeBinding Light=#E8E8E8, Dark=#2A2A2A}">
        <Label Grid.Column="0" Text="" />
        <Label Grid.Column="1" Text="Team"
               FontSize="11" FontAttributes="Bold"
               TextColor="{AppThemeBinding Light=#555555, Dark=#AAAAAA}"
               VerticalOptions="Center" />
        <Label Grid.Column="2" Text="Series"
               FontSize="11" FontAttributes="Bold"
               TextColor="{AppThemeBinding Light=#555555, Dark=#AAAAAA}"
               VerticalOptions="Center" />
        <Label Grid.Column="3" Text="Stats"
               FontSize="11" FontAttributes="Bold"
               TextColor="{AppThemeBinding Light=#555555, Dark=#AAAAAA}"
               HorizontalOptions="End"
               VerticalOptions="Center" />
    </Grid>

    <!-- Loading indicator -->
    <ActivityIndicator Grid.Row="2"
                       IsRunning="{Binding IsLoading}"
                       IsVisible="{Binding IsLoading}"
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />

    <!-- Empty / error state -->
    <Label Grid.Row="2"
           Text="{Binding StatusMessage}"
           IsVisible="{Binding StatusMessage, Converter={StaticResource StringNotEmptyConverter}}"
           HorizontalOptions="Center"
           VerticalOptions="Center"
           FontSize="14"
           TextColor="{AppThemeBinding Light=#666666, Dark=#999999}" />

    <!-- Rivalries list -->
    <CollectionView Grid.Row="2"
                    ItemsSource="{Binding Rivalries}"
                    IsVisible="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}">

      <CollectionView.ItemTemplate>
        <DataTemplate x:DataType="models:RivalryInfo">
          <VerticalStackLayout Spacing="0">

            <!-- Rivalry name + ★ follow game + tier badge -->
            <Grid ColumnDefinitions="*,Auto,Auto"
                  Padding="10,5,10,2">

              <Label Grid.Column="0"
                     Text="{Binding RivalryName}"
                     FontSize="12"
                     FontAttributes="Bold"
                     VerticalOptions="Center"
                     LineBreakMode="TailTruncation" />

              <!-- Star: follow this rivalry/game -->
              <controls:FollowGameIcon
                    Grid.Column="1"
                    IsFollowed="{Binding IsFollowed}"
                    VerticalOptions="Center"
                    HorizontalOptions="End"
                    Margin="8,0">
                    <controls:FollowGameIcon.GestureRecognizers>
                        <TapGestureRecognizer
                            Command="{Binding Source={x:Reference ThisPage}, Path=BindingContext.ToggleFollowRivalryCommand}"
                            CommandParameter="{Binding .}" />
                    </controls:FollowGameIcon.GestureRecognizers>
              </controls:FollowGameIcon>

              <Label Grid.Column="2"
                     Text="{Binding TierBadge}"
                     FontSize="16"
                     VerticalOptions="Center"
                     HorizontalOptions="End" />

            </Grid>

            <!-- Teams rows -->
            <Grid ColumnDefinitions="28,*,110,90"
                  RowDefinitions="Auto,Auto"
                  Padding="10,2,10,5">

              <!-- Heart: follow team 1 -->
              <controls:FollowTeamIcon
                    Grid.Row="0" Grid.Column="0"
                    IsFollowed="{Binding Team1IsFollowed}"
                    VerticalOptions="Center"
                    HorizontalOptions="Center">
                    <controls:FollowTeamIcon.GestureRecognizers>
                        <TapGestureRecognizer
                            Command="{Binding Source={x:Reference ThisPage}, Path=BindingContext.ToggleFollowCommand}"
                            CommandParameter="{Binding Team1Id}" />
                    </controls:FollowTeamIcon.GestureRecognizers>
              </controls:FollowTeamIcon>

              <!-- Team 1 name -->
              <Label Grid.Row="0" Grid.Column="1"
                     Text="{Binding Team1Name}"
                     FontSize="13"
                     VerticalOptions="Center"
                     LineBreakMode="TailTruncation" />

              <!-- Display series -->
              <Label Grid.Row="0" Grid.Column="2"
                     Text="{Binding DisplaySeries}"
                     FontSize="9"
                     TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                     VerticalOptions="Center" />

              <!-- Avg margin -->
              <Label Grid.Row="0" Grid.Column="3"
                     Text="{Binding DisplayAvgMargin}"
                     FontSize="9"
                     TextColor="{AppThemeBinding Light=#888888, Dark=#777777}"
                     VerticalOptions="Center"
                     HorizontalOptions="End" />

              <!-- Heart: follow team 2 -->
              <controls:FollowTeamIcon
                    Grid.Row="1" Grid.Column="0"
                    IsFollowed="{Binding Team2IsFollowed}"
                    VerticalOptions="Center"
                    HorizontalOptions="Center">
                    <controls:FollowTeamIcon.GestureRecognizers>
                        <TapGestureRecognizer
                            Command="{Binding Source={x:Reference ThisPage}, Path=BindingContext.ToggleFollowCommand}"
                            CommandParameter="{Binding Team2Id}" />
                    </controls:FollowTeamIcon.GestureRecognizers>
              </controls:FollowTeamIcon>

              <!-- Team 2 name -->
              <Label Grid.Row="1" Grid.Column="1"
                     Text="{Binding Team2Name}"
                     FontSize="13"
                     VerticalOptions="Center"
                     LineBreakMode="TailTruncation" />

              <!-- Upset rate -->
              <Label Grid.Row="1" Grid.Column="2"
                     Text="{Binding DisplayUpsetRate}"
                     FontSize="9"
                     TextColor="{AppThemeBinding Light=#666666, Dark=#999999}"
                     VerticalOptions="Center" />

              <!-- Avg swing -->
              <Label Grid.Row="1" Grid.Column="3"
                     Text="{Binding DisplayAvgSwing}"
                     FontSize="9"
                     TextColor="{AppThemeBinding Light=#888888, Dark=#777777}"
                     VerticalOptions="Center"
                     HorizontalOptions="End" />

            </Grid>

            <!-- Row divider -->
            <BoxView HeightRequest="1" Opacity="0.12"
                     Color="{AppThemeBinding Light=Black, Dark=White}" />

          </VerticalStackLayout>
        </DataTemplate>
      </CollectionView.ItemTemplate>

      <CollectionView.EmptyView>
        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="20">
          <Label Text="No rivalries found"
                 FontSize="16"
                 HorizontalOptions="Center" />
        </VerticalStackLayout>
      </CollectionView.EmptyView>

    </CollectionView>

    <!-- Status bar -->
    <Grid Grid.Row="3" HeightRequest="24"
          BackgroundColor="{AppThemeBinding Light=#f0f0f0, Dark=#2b2b2b}">
      <Label Text="{Binding StatusMessage}"
             Padding="8,0" FontSize="10" Opacity="0.7"
             VerticalOptions="Center" HorizontalOptions="Start" />
      <ActivityIndicator IsRunning="{Binding IsLoading}"
                         IsVisible="{Binding IsLoading}"
                         HeightRequest="16" WidthRequest="16"
                         HorizontalOptions="End" VerticalOptions="Center"
                         Margin="8,0" />
    </Grid>

  </Grid>
</ContentPage>
